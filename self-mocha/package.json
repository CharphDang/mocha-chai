{
  "scripts": {
    "test": "mocha --recursive --compilers js:babel-core/register",
    "test2": "mocha --recursive --require babel-core/register",
    "create-md": "mocha --recursive -R markdown > spec.md --require babel-core/register",
    "create-html": "mocha --recursive -R doc > spec.html --require babel-core/register"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^4.1.2",
    "mocha": "^5.2.0"
  }
}
